import type { Metadata } from "next";
import Architecture from "@/components/Architecture";

export const metadata: Metadata = {
  title: "Architecture — Lakshmiprabha",
};

export default function ArchitecturePage() {
  return <Architecture />;
}
